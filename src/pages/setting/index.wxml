<view class="c-bar"></view>

<pro-cell
  wx:for="{{ menus }}"
  wx:key="index"
  arrow="{{ item.arrow }}"
  label="{{ item.label }}"
  icon="{{ item.icon }}"
  bindtap="transfer(item)"
></pro-cell>

<button class="c-button is-mt" bindtap="handleLogout">退出账号</button>
